// DEPRECATED: the post-upload "confirmed orders alert" modal was dropped
// when UploadOrdersPage was rewritten (bulk upload now shows a summary +
// progress bar directly on the page instead). Kept as an empty stub (no
// delete access in this environment).
const ConfirmOrderAlertBadge = () => null;
export default ConfirmOrderAlertBadge;
