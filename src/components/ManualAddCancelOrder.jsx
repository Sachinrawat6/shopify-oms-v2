// DEPRECATED: called a hardcoded external URL unrelated to this project's
// backend. Replaced by the Cancelled Orders page's direct "add" flow. Kept
// as an empty stub (no delete access in this environment).
const ManualAddCancelOrder = () => null;
export default ManualAddCancelOrder;
