// DEPRECATED: replaced by components/layout/Sidebar.jsx, which uses
// react-router NavLinks instead of the old activeTab state pattern. Kept as
// an empty stub (no delete access in this environment).
const Sidebar = () => null;
export default Sidebar;
