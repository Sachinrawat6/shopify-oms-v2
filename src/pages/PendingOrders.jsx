// DEPRECATED: replaced by ./PendingOrdersPage.jsx as part of the MVC
// rewrite (routing, pagination, shared components, pending_reason field).
// Kept as an empty stub (no delete access in this environment).
const PendingOrders = () => null;
export default PendingOrders;
