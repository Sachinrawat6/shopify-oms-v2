// DEPRECATED: replaced by ./DashboardPage.jsx as part of the MVC rewrite
// (now backed by the /api/v1/dashboard/summary aggregation endpoint). Kept
// as an empty stub (no delete access in this environment).
const OrderDashboard = () => null;
export default OrderDashboard;
