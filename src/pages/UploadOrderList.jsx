// DEPRECATED: replaced by ./UploadOrdersPage.jsx as part of the MVC rewrite
// (adds price/payment_type fields, voided + blacklisted-orders persistence).
// Kept as an empty stub (no delete access in this environment).
const OrderUpload = () => null;
export default OrderUpload;
