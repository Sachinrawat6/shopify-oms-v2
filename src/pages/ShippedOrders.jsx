// DEPRECATED: the "Shipped / All Orders" concept was dropped in the rewrite
// -- it isn't part of the new sidebar/table spec, and Confirmed Orders is
// now the terminal state. Kept as an empty stub (no delete access in this
// environment) -- safe to delete manually.
const ShippedOrders = () => null;
export default ShippedOrders;
